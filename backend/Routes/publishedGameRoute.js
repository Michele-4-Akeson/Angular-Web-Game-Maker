require("dotenv/config"); 
const express = require("express");
const router = express.Router();
const publishedGame = require("../Models/publishedGameModel")

router.get("/", async function(req, res){

})

module.exports = router;